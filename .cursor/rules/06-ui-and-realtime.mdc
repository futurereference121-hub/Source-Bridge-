---
description: Chat Payment Ticket UI, collapse, polling, and viewer isolation
globs: src/components/messaging/**,src/components/payments/**
alwaysApply: false
---

# Chat, UI, and realtime

Conversation is primarily messaging. Payment Tickets must not overwhelm chat. All tickets collapse/expand; **critical actions stay accessible**.

- Unfunded cancelled/declined/superseded: hidden from normal chat.
- Funded history: remains, compact/collapsible when completed.
- Header **Payment Ticket** control stays near View Profile.
- Preserve ~**2.5s** active soft-poll (or improve it). Do not add a competing realtime system.

Polling / revalidation must not: clear drafts, reset collapse keyed by ticket id, jump scroll, duplicate messages/tickets, or repeat financial actions.

## Performance (permanent principle)

User-perceived speed is part of definition of done. Prefer monotonic `activityVersion` + stale-response guards over extra polling, spinners, or refresh-after-click. One lightweight active-conversation poll; mutation responses return canonical ticket state immediately for the acting user. Never apply older poll/mutation payloads over newer funded or accepted state.

## Viewer

Actions derive from the **current authenticated session** + authoritative ticket fields. Conversation GET `viewerUserId` (cookie session `User.id`) drives Payment Ticket Accept — never a cached `/api/auth/me` identity from another party. Never reuse another user’s `ticket.viewer` / actions / conversation payload. Authenticated ticket/conversation/`/api/auth/me` responses: `private, no-store`.

## Receipt UI

Protected: after item received, buyer chooses **Release Funds Now** or **Start 12-Hour Inspection**. During inspection: Release Now + Report a Problem. Issue freeze requires admin review. Do not restore a forced 48-hour period unless explicitly requested.
